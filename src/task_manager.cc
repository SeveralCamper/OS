#include <task_manager>

void taskManager() {
    std::cout << "Pleasa, enter task manager operation key" << std::endl;
    while (true) {
        if (parse_string)
    }

}

void parseManagerKey(std::string parse_string) {
}
